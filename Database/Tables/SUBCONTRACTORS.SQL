CREATE TABLE subcontractors (
    SUBCONTRACTOR_ID NOT NULL PRIMARY KEY,
    DATE_CREATED DATETIME NOT NULL,
    LAST_UPDATE DATETIME NOT NULL,
    NAME TEXT,
    EIN TEXT,
    STREET TEXT,
    CITY TEXT,
    STATE TEXT,
    ZIP TEXT
);